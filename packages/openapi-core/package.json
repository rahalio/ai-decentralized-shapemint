{
  "name": "@shapemint/openapi-core",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI contracts for Shapemint (common + domains)",
  "scripts": {
    "lint:domains": "redocly lint",
    "bundle:domains": "mkdir -p src/.bundled && redocly bundle identity --output src/.bundled/identity.openapi.yaml && redocly bundle identity --output src/.bundled/identity.json && redocly bundle models --output src/.bundled/models.openapi.yaml && redocly bundle models --output src/.bundled/models.json && redocly bundle contributions --output src/.bundled/contributions.openapi.yaml && redocly bundle contributions --output src/.bundled/contributions.json && redocly bundle influence --output src/.bundled/influence.openapi.yaml && redocly bundle influence --output src/.bundled/influence.json && redocly bundle protocols --output src/.bundled/protocols.openapi.yaml && redocly bundle protocols --output src/.bundled/protocols.json && redocly bundle incentives --output src/.bundled/incentives.openapi.yaml && redocly bundle incentives --output src/.bundled/incentives.json && redocly bundle governance --output src/.bundled/governance.openapi.yaml && redocly bundle governance --output src/.bundled/governance.json && redocly bundle reporting --output src/.bundled/reporting.openapi.yaml && redocly bundle reporting --output src/.bundled/reporting.json",
    "lint": "pnpm run lint:domains",
    "bundle": "pnpm run bundle:domains"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.0"
  },
  "engines": {
    "node": ">=18"
  }
}
