/**
 * ProtocolRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: protocols
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ProtocolRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements ProtocolRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  ProtocolRepository,
} from "@shapemint/services/protocols";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { ProtocolRepositoryDdb } from "./protocol-repository.ddb.js";

/**
 * ProtocolRepositoryAdapter
 *
 * Implements ProtocolRepository port for infrastructure layer.
 */
export class ProtocolRepositoryAdapter implements ProtocolRepository {
  private readonly ddb: ProtocolRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new ProtocolRepositoryDdb(this.dynamoClient);
  }

  async listProtocols(input: Parameters<ProtocolRepository['listProtocols']>[0]): Promise<Awaited<ReturnType<ProtocolRepository['listProtocols']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listProtocols(input);
  }

  async createProtocol(input: Parameters<ProtocolRepository['createProtocol']>[0]): Promise<Awaited<ReturnType<ProtocolRepository['createProtocol']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.createProtocol(input);
  }

  async getProtocol(input: Parameters<ProtocolRepository['getProtocol']>[0]): Promise<Awaited<ReturnType<ProtocolRepository['getProtocol']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getProtocol(input);
  }
}
