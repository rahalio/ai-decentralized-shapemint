/**
 * Governance DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Governance Domain Module - Composition root
 *
 * DDD: Composition root for governance domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  MintingPauseRepositoryAdapter,
  NegativeInfluenceRepositoryAdapter,
  ResumeRepositoryAdapter,
  TransparencyGrantRepositoryAdapter,
} from "@shapemint/adapters/governance";
import { getIdGeneratorService } from "@shapemint/adapters";
import type { AdapterDynamoDBClient } from "@shapemint/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateTransparencyGrant,
  ExecuteListMintingPauses,
  ExecuteListNegativeInfluenceRulings,
  ExecuteListTransparencyGrants,
  ExecutePauseMinting,
  ExecuteResumeMinting,
  ExecuteRuleNegativeInfluence,
} from "@shapemint/services/governance/usecases";
// Ports (interfaces)
import type {
  MintingPauseRepository,
  NegativeInfluenceRepository,
  ResumeRepository,
  TransparencyGrantRepository,
} from "@shapemint/services/governance/ports";

export interface GovernanceDomainModule {
  repos: {
    mintingPauses: MintingPauseRepository;
    negativeInfluences: NegativeInfluenceRepository;
    resumes: ResumeRepository;
    transparencyGrants: TransparencyGrantRepository;
  };
  useCases: {
    mintingPauses: {
      get: ExecutePauseMinting;
      list: ExecuteListMintingPauses;
    };
    negativeInfluences: {
      get: ExecuteRuleNegativeInfluence;
      list: ExecuteListNegativeInfluenceRulings;
    };
    resumes: {
      get: ExecuteResumeMinting;
    };
    transparencyGrants: {
      create: ExecuteCreateTransparencyGrant;
      list: ExecuteListTransparencyGrants;
    };
  };
}

export function buildGovernanceDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): GovernanceDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      mintingPauses?: MintingPauseRepository;
      negativeInfluences?: NegativeInfluenceRepository;
      resumes?: ResumeRepository;
      transparencyGrants?: TransparencyGrantRepository;
    } = {};

    obj.mintingPauses = new MintingPauseRepositoryAdapter(dynamoClient);
    obj.negativeInfluences = new NegativeInfluenceRepositoryAdapter(dynamoClient);
    obj.resumes = new ResumeRepositoryAdapter(dynamoClient);
    obj.transparencyGrants = new TransparencyGrantRepositoryAdapter(dynamoClient);
    return {
      mintingPauses: obj.mintingPauses!,
      negativeInfluences: obj.negativeInfluences!,
      resumes: obj.resumes!,
      transparencyGrants: obj.transparencyGrants!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    mintingPauses: {
      get: new ExecutePauseMinting(executionContext, idGenerator, repos.mintingPauses),
      list: new ExecuteListMintingPauses(executionContext, idGenerator, repos.mintingPauses),
    },
    negativeInfluences: {
      get: new ExecuteRuleNegativeInfluence(executionContext, idGenerator, repos.negativeInfluences),
      list: new ExecuteListNegativeInfluenceRulings(executionContext, idGenerator, repos.negativeInfluences),
    },
    resumes: {
      get: new ExecuteResumeMinting(executionContext, idGenerator, repos.resumes),
    },
    transparencyGrants: {
      create: new ExecuteCreateTransparencyGrant(executionContext, idGenerator, repos.transparencyGrants),
      list: new ExecuteListTransparencyGrants(executionContext, idGenerator, repos.transparencyGrants),
    },
  };
  return { repos, useCases: useCases as GovernanceDomainModule["useCases"] };
}
