/**
 * Incentives DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incentives
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Incentives Domain Module - Composition root
 *
 * DDD: Composition root for incentives domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AcceptRepositoryAdapter,
  ClawbackRepositoryAdapter,
  IncentivePolicyRepositoryAdapter,
  IncentiveRepositoryAdapter,
  SettlementStatementRepositoryAdapter,
} from "@shapemint/adapters/incentives";
import { getIdGeneratorService } from "@shapemint/adapters";
import type { AdapterDynamoDBClient } from "@shapemint/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAcceptIncentivePolicy,
  ExecuteClawbackIncentive,
  ExecuteCreateIncentivePolicy,
  ExecuteCreateSettlementStatement,
  ExecuteGetIncentivePolicy,
  ExecuteGetSettlementStatement,
  ExecuteListIncentivePolicies,
  ExecuteListIncentives,
  ExecuteListSettlementStatements,
  ExecuteMintIncentive,
} from "@shapemint/services/incentives/usecases";
// Ports (interfaces)
import type {
  AcceptRepository,
  ClawbackRepository,
  IncentivePolicyRepository,
  IncentiveRepository,
  SettlementStatementRepository,
} from "@shapemint/services/incentives/ports";

export interface IncentivesDomainModule {
  repos: {
    accepts: AcceptRepository;
    clawbacks: ClawbackRepository;
    incentivePolicies: IncentivePolicyRepository;
    incentives: IncentiveRepository;
    settlementStatements: SettlementStatementRepository;
  };
  useCases: {
    accepts: {
      get: ExecuteAcceptIncentivePolicy;
    };
    clawbacks: {
      get: ExecuteClawbackIncentive;
    };
    incentivePolicies: {
      create: ExecuteCreateIncentivePolicy;
      get: ExecuteGetIncentivePolicy;
      list: ExecuteListIncentivePolicies;
    };
    incentives: {
      get: ExecuteMintIncentive;
      list: ExecuteListIncentives;
    };
    settlementStatements: {
      create: ExecuteCreateSettlementStatement;
      get: ExecuteGetSettlementStatement;
      list: ExecuteListSettlementStatements;
    };
  };
}

export function buildIncentivesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): IncentivesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      accepts?: AcceptRepository;
      clawbacks?: ClawbackRepository;
      incentivePolicies?: IncentivePolicyRepository;
      incentives?: IncentiveRepository;
      settlementStatements?: SettlementStatementRepository;
    } = {};

    obj.accepts = new AcceptRepositoryAdapter(dynamoClient);
    obj.clawbacks = new ClawbackRepositoryAdapter(dynamoClient);
    obj.incentivePolicies = new IncentivePolicyRepositoryAdapter(dynamoClient);
    obj.incentives = new IncentiveRepositoryAdapter(dynamoClient);
    obj.settlementStatements = new SettlementStatementRepositoryAdapter(dynamoClient);
    return {
      accepts: obj.accepts!,
      clawbacks: obj.clawbacks!,
      incentivePolicies: obj.incentivePolicies!,
      incentives: obj.incentives!,
      settlementStatements: obj.settlementStatements!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    accepts: {
      get: new ExecuteAcceptIncentivePolicy(executionContext, idGenerator, repos.accepts),
    },
    clawbacks: {
      get: new ExecuteClawbackIncentive(executionContext, idGenerator, repos.clawbacks),
    },
    incentivePolicies: {
      create: new ExecuteCreateIncentivePolicy(executionContext, idGenerator, repos.incentivePolicies),
      get: new ExecuteGetIncentivePolicy(executionContext, idGenerator, repos.incentivePolicies),
      list: new ExecuteListIncentivePolicies(executionContext, idGenerator, repos.incentivePolicies),
    },
    incentives: {
      get: new ExecuteMintIncentive(executionContext, idGenerator, repos.incentives),
      list: new ExecuteListIncentives(executionContext, idGenerator, repos.incentives),
    },
    settlementStatements: {
      create: new ExecuteCreateSettlementStatement(executionContext, idGenerator, repos.settlementStatements),
      get: new ExecuteGetSettlementStatement(executionContext, idGenerator, repos.settlementStatements),
      list: new ExecuteListSettlementStatements(executionContext, idGenerator, repos.settlementStatements),
    },
  };
  return { repos, useCases: useCases as IncentivesDomainModule["useCases"] };
}
