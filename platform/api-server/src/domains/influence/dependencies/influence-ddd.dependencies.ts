/**
 * Influence DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: influence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Influence Domain Module - Composition root
 *
 * DDD: Composition root for influence domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AcceptRepositoryAdapter,
  InfluenceRepositoryAdapter,
  LedgerRepositoryAdapter,
  RejectRepositoryAdapter,
  ReproducePackRepositoryAdapter,
} from "@shapemint/adapters/influence";
import { getIdGeneratorService } from "@shapemint/adapters";
import type { AdapterDynamoDBClient } from "@shapemint/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAcceptInfluence,
  ExecuteGetInfluence,
  ExecuteGetReproducePack,
  ExecuteListInfluenceLedger,
  ExecuteRejectInfluence,
  ExecuteScoreInfluence,
} from "@shapemint/services/influence/usecases";
// Ports (interfaces)
import type {
  AcceptRepository,
  InfluenceRepository,
  LedgerRepository,
  RejectRepository,
  ReproducePackRepository,
} from "@shapemint/services/influence/ports";

export interface InfluenceDomainModule {
  repos: {
    accepts: AcceptRepository;
    influences: InfluenceRepository;
    ledgers: LedgerRepository;
    rejects: RejectRepository;
    reproducePacks: ReproducePackRepository;
  };
  useCases: {
    accepts: {
      get: ExecuteAcceptInfluence;
    };
    influences: {
      get: ExecuteScoreInfluence;
    };
    ledgers: {
      list: ExecuteListInfluenceLedger;
    };
    rejects: {
      create: ExecuteRejectInfluence;
    };
    reproducePacks: {
      get: ExecuteGetReproducePack;
    };
  };
}

export function buildInfluenceDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): InfluenceDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      accepts?: AcceptRepository;
      influences?: InfluenceRepository;
      ledgers?: LedgerRepository;
      rejects?: RejectRepository;
      reproducePacks?: ReproducePackRepository;
    } = {};

    obj.accepts = new AcceptRepositoryAdapter(dynamoClient);
    obj.influences = new InfluenceRepositoryAdapter(dynamoClient);
    obj.ledgers = new LedgerRepositoryAdapter(dynamoClient);
    obj.rejects = new RejectRepositoryAdapter(dynamoClient);
    obj.reproducePacks = new ReproducePackRepositoryAdapter(dynamoClient);
    return {
      accepts: obj.accepts!,
      influences: obj.influences!,
      ledgers: obj.ledgers!,
      rejects: obj.rejects!,
      reproducePacks: obj.reproducePacks!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    accepts: {
      get: new ExecuteAcceptInfluence(executionContext, idGenerator, repos.accepts),
    },
    influences: {
      get: new ExecuteScoreInfluence(executionContext, idGenerator, repos.influences),
    },
    ledgers: {
      list: new ExecuteListInfluenceLedger(executionContext, idGenerator, repos.ledgers),
    },
    rejects: {
      create: new ExecuteRejectInfluence(executionContext, idGenerator, repos.rejects),
    },
    reproducePacks: {
      get: new ExecuteGetReproducePack(executionContext, idGenerator, repos.reproducePacks),
    },
  };
  return { repos, useCases: useCases as InfluenceDomainModule["useCases"] };
}
