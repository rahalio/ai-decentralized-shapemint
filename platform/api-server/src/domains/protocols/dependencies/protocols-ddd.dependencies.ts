/**
 * Protocols DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: protocols
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Protocols Domain Module - Composition root
 *
 * DDD: Composition root for protocols domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  BindModelRepositoryAdapter,
  DeprecateRepositoryAdapter,
  ProtocolRepositoryAdapter,
} from "@shapemint/adapters/protocols";
import { getIdGeneratorService } from "@shapemint/adapters";
import type { AdapterDynamoDBClient } from "@shapemint/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteBindProtocolToModel,
  ExecuteCreateProtocol,
  ExecuteDeprecateProtocol,
  ExecuteGetProtocol,
  ExecuteListProtocols,
  ExecutePublishProtocol,
} from "@shapemint/services/protocols/usecases";
// Ports (interfaces)
import type {
  BindModelRepository,
  DeprecateRepository,
  ProtocolRepository,
} from "@shapemint/services/protocols/ports";

export interface ProtocolsDomainModule {
  repos: {
    bindModels: BindModelRepository;
    deprecates: DeprecateRepository;
    protocols: ProtocolRepository;
  };
  useCases: {
    bindModels: {
      get: ExecuteBindProtocolToModel;
    };
    deprecates: {
      get: ExecuteDeprecateProtocol;
    };
    protocols: {
      create: ExecuteCreateProtocol;
      get: ExecuteGetProtocol;
      list: ExecuteListProtocols;
    };
  };
}

export function buildProtocolsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ProtocolsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      bindModels?: BindModelRepository;
      deprecates?: DeprecateRepository;
      protocols?: ProtocolRepository;
    } = {};

    obj.bindModels = new BindModelRepositoryAdapter(dynamoClient);
    obj.deprecates = new DeprecateRepositoryAdapter(dynamoClient);
    obj.protocols = new ProtocolRepositoryAdapter(dynamoClient);
    return {
      bindModels: obj.bindModels!,
      deprecates: obj.deprecates!,
      protocols: obj.protocols!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    bindModels: {
      get: new ExecuteBindProtocolToModel(executionContext, idGenerator, repos.bindModels),
    },
    deprecates: {
      get: new ExecuteDeprecateProtocol(executionContext, idGenerator, repos.deprecates),
    },
    protocols: {
      create: new ExecuteCreateProtocol(executionContext, idGenerator, repos.protocols),
      get: new ExecuteGetProtocol(executionContext, idGenerator, repos.protocols),
      list: new ExecuteListProtocols(executionContext, idGenerator, repos.protocols),
    },
  };
  return { repos, useCases: useCases as ProtocolsDomainModule["useCases"] };
}
