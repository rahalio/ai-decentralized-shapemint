/**
 * ExecuteGetContribution - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contributions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetContribution Use Case
 *
 * DDD: Application use case for get Contribution.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetContributionInput, GetContributionOutput } from "../dto/contribution.dto";
import type { ExecutionContextService, IdGeneratorService } from "@shapemint/services/_shared/index.js";
import type { ContributionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetContribution {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly contribution: ContributionRepository
  ) {}

  async execute(input: GetContributionInput): Promise<GetContributionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctrId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).contributionId) {
      throw new ValidationError("contributionId is required");
    }
    const contributionId = (input as any).contributionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.contribution.getContribution({ contributionId: contributionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${contributionId}`);
    }

    return entity as GetContributionOutput;
  }
}
