/**
 * ExecuteListContributions - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contributions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListContributions Use Case
 *
 * DDD: Application use case for list Contribution.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListContributionsInput, ListContributionsOutput } from "../dto/contribution.dto";
import type { ExecutionContextService, IdGeneratorService } from "@shapemint/services/_shared/index.js";
import type { ContributionRepository } from "../ports";

export class ExecuteListContributions {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly contribution: ContributionRepository
  ) {}

  async execute(input: ListContributionsInput): Promise<ListContributionsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctrId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.contribution.listContributions({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListContributionsOutput;
  }
}
