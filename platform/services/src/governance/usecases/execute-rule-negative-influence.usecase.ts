/**
 * ExecuteRuleNegativeInfluence - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRuleNegativeInfluence Use Case
 *
 * DDD: Application use case for get NegativeInfluence.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RuleNegativeInfluenceInput, RuleNegativeInfluenceOutput } from "../dto/negative-influence.dto";
import type { ExecutionContextService, IdGeneratorService } from "@shapemint/services/_shared/index.js";
import type { NegativeInfluenceRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRuleNegativeInfluence {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly negativeInfluence: NegativeInfluenceRepository
  ) {}

  async execute(input: RuleNegativeInfluenceInput): Promise<RuleNegativeInfluenceOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.govId();

    // All field names extracted from OpenAPI spec: contributionId, modelId, outcome, reason
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).contributionId) {
      throw new ValidationError("contributionId is required");
    }
    const contributionId = (input as any).contributionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.negativeInfluence.ruleNegativeInfluence({ contributionId: contributionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${contributionId}`);
    }

    return entity as RuleNegativeInfluenceOutput;
  }
}
