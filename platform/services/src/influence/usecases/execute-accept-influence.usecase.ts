/**
 * ExecuteAcceptInfluence - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: influence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAcceptInfluence Use Case
 *
 * DDD: Application use case for get Accept.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AcceptInfluenceInput, AcceptInfluenceOutput } from "../dto/accept.dto";
import type { ExecutionContextService, IdGeneratorService } from "@shapemint/services/_shared/index.js";
import type { AcceptRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAcceptInfluence {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly accept: AcceptRepository
  ) {}

  async execute(input: AcceptInfluenceInput): Promise<AcceptInfluenceOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.infId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).influenceScoreId) {
      throw new ValidationError("influenceScoreId is required");
    }
    const influencescoreId = (input as any).influenceScoreId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.accept.acceptInfluence({ influenceScoreId: influencescoreId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${influencescoreId}`);
    }

    return entity as AcceptInfluenceOutput;
  }
}
