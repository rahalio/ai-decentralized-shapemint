/**
 * ExecuteBindProtocolToModel - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: protocols
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteBindProtocolToModel Use Case
 *
 * DDD: Application use case for get BindModel.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { BindProtocolToModelInput, BindProtocolToModelOutput } from "../dto/bind-model.dto";
import type { ExecutionContextService, IdGeneratorService } from "@shapemint/services/_shared/index.js";
import type { BindModelRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteBindProtocolToModel {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly bindModel: BindModelRepository
  ) {}

  async execute(input: BindProtocolToModelInput): Promise<BindProtocolToModelOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prtId();

    // All field names extracted from OpenAPI spec: modelId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).protocolId) {
      throw new ValidationError("protocolId is required");
    }
    const protocolId = (input as any).protocolId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.bindModel.bindProtocolToModel({ protocolId: protocolId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${protocolId}`);
    }

    return entity as BindProtocolToModelOutput;
  }
}
