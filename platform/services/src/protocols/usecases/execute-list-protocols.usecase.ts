/**
 * ExecuteListProtocols - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: protocols
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListProtocols Use Case
 *
 * DDD: Application use case for list Protocol.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListProtocolsInput, ListProtocolsOutput } from "../dto/protocol.dto";
import type { ExecutionContextService, IdGeneratorService } from "@shapemint/services/_shared/index.js";
import type { ProtocolRepository } from "../ports";

export class ExecuteListProtocols {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly protocol: ProtocolRepository
  ) {}

  async execute(input: ListProtocolsInput): Promise<ListProtocolsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prtId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.protocol.listProtocols({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListProtocolsOutput;
  }
}
