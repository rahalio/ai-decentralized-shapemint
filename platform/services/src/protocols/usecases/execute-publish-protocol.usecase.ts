/**
 * ExecutePublishProtocol - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: protocols
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecutePublishProtocol Use Case
 *
 * DDD: Application use case for get Publish.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { PublishProtocolInput, PublishProtocolOutput } from "../dto/publish.dto";
import type { ExecutionContextService, IdGeneratorService } from "@shapemint/services/_shared/index.js";
import type { ProtocolRepository, PublishPublisher } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecutePublishProtocol {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly publish: PublishPublisher,
    private readonly protocol: ProtocolRepository
  ) {}

  async execute(input: PublishProtocolInput): Promise<PublishProtocolOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prtId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).protocolId) {
      throw new ValidationError("protocolId is required");
    }
    const protocolId = (input as any).protocolId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.publish.publishProtocol({ protocolId: protocolId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${protocolId}`);
    }

    return entity as PublishProtocolOutput;
  }
}
