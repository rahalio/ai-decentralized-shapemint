/**
 * Contribution Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contributions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { ContributionEntity } from "@shapemint/core/contributions/models/index.js";
import { contributionsSchemas } from "@shapemint/core/contributions";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "ctr";

export function createContribution(overrides: Partial<ContributionEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    contributionId: overrides.contributionId ?? `ctr_${ulid().toLowerCase()}`,
    contributorId: overrides.contributorId ?? `usr_${ulid().toLowerCase()}`,
    kind: overrides.kind ?? "model_update",
    modelId: overrides.modelId ?? `mdl_${ulid().toLowerCase()}`,
    policyAccepted: overrides.policyAccepted ?? faker.datatype.boolean(),
    privacyMode: overrides.privacyMode ?? faker.datatype.boolean(),
    status: overrides.status ?? "draft",
    ...overrides,
  };

  return contributionsSchemas.Contribution.parse(raw);
}

export function contributionBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createContribution(overrides));
}
