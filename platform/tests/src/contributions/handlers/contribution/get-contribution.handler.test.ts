/**
 * GetContribution handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contributions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getContribution } from "@shapemint/api-server/domains/contributions/handlers";
import type { ContributionsDDDDependencies } from "@shapemint/api-server/domains/contributions";
import { createContribution as createContributionEntity } from "@shapemint/factories/contributions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shapemint/tests/shared";
import { CONTRIBUTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetContribution", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ContributionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ContributionsDDDDependencies>(
      CONTRIBUTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createContributionEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetContribution, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { contributionId: entity.id };

    await getContribution(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetContribution.execute).toHaveBeenCalledWith(expect.objectContaining({ contributionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { contributionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetContribution, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getContribution(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
