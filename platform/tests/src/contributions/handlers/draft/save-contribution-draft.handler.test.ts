/**
 * SaveContributionDraft handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contributions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { saveContributionDraft } from "@shapemint/api-server/domains/contributions/handlers";
import type { ContributionsDDDDependencies } from "@shapemint/api-server/domains/contributions";
import { createCreateDraftRequest } from "@shapemint/factories/contributions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shapemint/tests/shared";
import { CONTRIBUTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("SaveContributionDraft", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ContributionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ContributionsDDDDependencies>(
      CONTRIBUTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateDraftRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSaveContributionDraft, "execute").mockResolvedValue(useCaseResult);

    await saveContributionDraft(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSaveContributionDraft.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
