/**
 * ClawbackIncentive handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incentives
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { clawbackIncentive } from "@shapemint/api-server/domains/incentives/handlers";
import type { IncentivesDDDDependencies } from "@shapemint/api-server/domains/incentives";
import { createCreateClawbackRequest } from "@shapemint/factories/incentives";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shapemint/tests/shared";
import { INCENTIVES_USE_CASES } from "../__factories__/use-cases.js";

describe("ClawbackIncentive", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: IncentivesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<IncentivesDDDDependencies>(
      INCENTIVES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateClawbackRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeClawbackIncentive, "execute").mockResolvedValue(useCaseResult);

    await clawbackIncentive(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeClawbackIncentive.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
