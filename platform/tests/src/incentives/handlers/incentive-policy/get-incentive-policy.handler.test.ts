/**
 * GetIncentivePolicy handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incentives
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getIncentivePolicy } from "@shapemint/api-server/domains/incentives/handlers";
import type { IncentivesDDDDependencies } from "@shapemint/api-server/domains/incentives";
import { createIncentivePolicy as createIncentivePolicyEntity } from "@shapemint/factories/incentives";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shapemint/tests/shared";
import { INCENTIVES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetIncentivePolicy", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: IncentivesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<IncentivesDDDDependencies>(
      INCENTIVES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createIncentivePolicyEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetIncentivePolicy, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { policyId: entity.id };

    await getIncentivePolicy(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetIncentivePolicy.execute).toHaveBeenCalledWith(expect.objectContaining({ policyId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { policyId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetIncentivePolicy, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getIncentivePolicy(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
