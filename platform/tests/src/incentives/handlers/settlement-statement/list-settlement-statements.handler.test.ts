/**
 * ListSettlementStatements handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incentives
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listSettlementStatements } from "@shapemint/api-server/domains/incentives/handlers";
import type { IncentivesDDDDependencies } from "@shapemint/api-server/domains/incentives";
import { createSettlementStatement as createSettlementStatementEntity } from "@shapemint/factories/incentives";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shapemint/tests/shared";
import { INCENTIVES_USE_CASES } from "../__factories__/use-cases.js";

describe("ListSettlementStatements", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: IncentivesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<IncentivesDDDDependencies>(
      INCENTIVES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createSettlementStatementEntity({ orgId }),
      createSettlementStatementEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListSettlementStatements, "execute").mockResolvedValue(useCaseResult);

    await listSettlementStatements(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListSettlementStatements.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
