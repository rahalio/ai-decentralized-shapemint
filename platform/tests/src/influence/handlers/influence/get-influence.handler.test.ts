/**
 * GetInfluence handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: influence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getInfluence } from "@shapemint/api-server/domains/influence/handlers";
import type { InfluenceDDDDependencies } from "@shapemint/api-server/domains/influence";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shapemint/tests/shared";
import { INFLUENCE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetInfluence", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: InfluenceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<InfluenceDDDDependencies>(
      INFLUENCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetInfluence, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { contributionId: entity.id };

    await getInfluence(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetInfluence.execute).toHaveBeenCalledWith(expect.objectContaining({ contributionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { contributionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetInfluence, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getInfluence(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
