/**
 * GetReproducePack handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: influence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getReproducePack } from "@shapemint/api-server/domains/influence/handlers";
import type { InfluenceDDDDependencies } from "@shapemint/api-server/domains/influence";
import { createReproducePack as createReproducePackEntity } from "@shapemint/factories/influence";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shapemint/tests/shared";
import { INFLUENCE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetReproducePack", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: InfluenceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<InfluenceDDDDependencies>(
      INFLUENCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createReproducePackEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetReproducePack, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { influenceScoreId: entity.id };

    await getReproducePack(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetReproducePack.execute).toHaveBeenCalledWith(expect.objectContaining({ influenceScoreId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { influenceScoreId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetReproducePack, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getReproducePack(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
